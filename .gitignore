node_modules/
.DS_Store
*.log
.vercel
.env
*.env

# Alumni data exports — never commit personal data
*.csv
!sample_alumni_import_template.csv
_tmp_*

# Tool / scan output directories
.playwright-mcp/
CLAUDE-SECURITY-*/

# Baseline artifacts produced by tools/ (regenerate with npm run contract)
# committed on purpose: the API contract baseline to diff against
!tools/api-contract.baseline.txt

# Any .env variant (.env.local, .env.bak.<ts>, …) — never commit secrets
.env*
!.env.example

# Business documents — this remote is public. Keep contracts, negotiation
# strategy and client-facing PDFs out of the repository.
legal/
*.pdf
*.docx
.claude/
