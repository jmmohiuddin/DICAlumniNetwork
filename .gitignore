node_modules/
.DS_Store
*.log
.vercel
.env
*.env

# Alumni data exports — never commit personal data
*.csv
!sample_alumni_import_template.csv
_tmp_*

# Tool / scan output directories
.playwright-mcp/
CLAUDE-SECURITY-*/

# Baseline artifacts produced by tools/ (regenerate with npm run contract)
# committed on purpose: the API contract baseline to diff against
!tools/api-contract.baseline.txt
